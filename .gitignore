.netlify/
node_modules/
.env
.env.*
!.env.example
# Local-only IndexNow queues. Production submission derives its URL list from
# the merge diff (.seo-changed-urls.json, built by the SEO workflow), never from
# a committed, mutable queue that a deterministic rebuild could erase.
.indexnow-urls.json
.seo-changed-urls.json
